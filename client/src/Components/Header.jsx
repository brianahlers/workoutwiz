import React from 'react';
import Nav from './Nav';

const Header = () => {
    return (
        <header>
            <Nav />
            <p>this might only hold the nav, not sure yet</p>
        </header>
    );
};

export default Header;